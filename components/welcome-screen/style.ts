import { Platform, StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  imageBox: {
    marginTop: 70,
    alignItems: "center",
    justifyContent: "center",
  },
  title: { fontSize: 32, fontWeight: "900", color: "#0C359E" },
  description: {fontSize: 20, fontWeight: "400", color: "#0C359E"},
  contentBox: {
    marginTop: 80,
    paddingHorizontal: 20,
    justifyContent: "center",
    alignItems: "center",
    gap: 20,
  },
});
